# Lab 606: factor repeated distance wording

Move the repeated straight-line distance explanation from option labels into one shared question instruction. Each matching option retains its numeric minimum/maximum as distance_range=[min,max]. Keys, ordering, descriptions outside that exact phrase, targets and commands are unchanged. The transformation is idempotent for recursive tournament calls. Unknown formats pass through.

Recorded trial604 loop733 MobileCombat:151options,39876serialized characters before,35721after (10.4% smaller). This is a representation-size measurement, not token billing or proof that the159-option rejected request will fit. Full-menu request is still attempted; existing loud failures and Jev tournament fallback remain.

Validation:94 tests passed across distance-label tests and infrastructure tests. Updated one existing test to assert preserved distance numbers and the relocated route-uncertainty statement. This player change live-reloads on commit; trial604 therefore becomes a mixed-policy run after this commit. No win or request-count benefit is claimed yet.
